export const METADATA_CHAIN_PARAM = 'chain:param';
export const METADATA_CHAINS_LIST = 'chains:name:list';

export const TARGET_CHAINS_LISTS = Object.seal({});
export const TARGET_CHAIN_PARAMS = Object.seal({});

